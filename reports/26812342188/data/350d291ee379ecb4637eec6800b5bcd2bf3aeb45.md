# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: category-archive.spec.ts >> Category Archive Page >> should return 404 for nonexistent category
- Location: tests/e2e/category-archive.spec.ts:42:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
TimeoutError: page.goto: Timeout 30000ms exceeded.
Call log:
  - navigating to "https://pr-58-jazz-nextjs15.pantheonsite.io/category/this-category-does-not-exist-xyz", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | /**
  4  |  * Category archive — /category/[slug]
  5  |  *
  6  |  * Uses "ministry-of-music" — 183 posts, reliable test target.
  7  |  */
  8  | const testCategory = 'ministry-of-music'
  9  | 
  10 | test.describe('Category Archive Page', () => {
  11 |   test('should display category heading', async ({ page }) => {
  12 |     await page.goto(`/category/${testCategory}`)
  13 |     await page.waitForLoadState('domcontentloaded')
  14 | 
  15 |     const h1 = page.locator('h1')
  16 |     await expect(h1).toBeVisible()
  17 |     await expect(h1).not.toBeEmpty()
  18 |   })
  19 | 
  20 |   test('should display post list', async ({ page }) => {
  21 |     await page.goto(`/category/${testCategory}`)
  22 |     await page.waitForLoadState('domcontentloaded')
  23 | 
  24 |     const posts = page.locator('article, [data-testid="post-card"]')
  25 |     await expect(posts.first()).toBeVisible()
  26 |   })
  27 | 
  28 |   test('should have navigation', async ({ page }) => {
  29 |     await page.goto(`/category/${testCategory}`)
  30 |     await page.waitForLoadState('domcontentloaded')
  31 | 
  32 |     await expect(page.locator('nav[role="navigation"]')).toBeVisible()
  33 |   })
  34 | 
  35 |   test('should have footer', async ({ page }) => {
  36 |     await page.goto(`/category/${testCategory}`)
  37 |     await page.waitForLoadState('domcontentloaded')
  38 | 
  39 |     await expect(page.locator('footer')).toBeVisible()
  40 |   })
  41 | 
  42 |   test('should return 404 for nonexistent category', async ({ page }) => {
> 43 |     const response = await page.goto('/category/this-category-does-not-exist-xyz')
     |                                 ^ TimeoutError: page.goto: Timeout 30000ms exceeded.
  44 |     expect(response?.status()).toBe(404)
  45 |   })
  46 | })
  47 | 
```