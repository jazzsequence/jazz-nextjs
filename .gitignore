# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/
/.cache/

# production
/build

# misc
.DS_Store
*.pem

# Internal research and analysis
.research/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.lighthouse/

# env files (can opt-in for committing if needed)
.env*
!config/.env.local.example

# Claude Code & MCP local configuration
.claude/
.claude-flow/
.mcp.json
.reviewer-approval

# Session working files (never commit)
docs/SESSION_NOTES.md
docs/EOD_SESSION_NOTES_*.md
**/session-notes*.md

# typescript
*.tsbuildinfo
next-env.d.ts

#Ignore vscode AI rules
.github/instructions/codacy.instructions.md
secrets.json
# next-agents-md
.next-docs/
# Reviewer install state — records the hash of the INSTALLED hook copy, which is
# per-machine. The tracked sources in .githooks/ are the shared artifact.
.reviewer-manifest
.git/hooks/pre-commit.bak

reviewer-approved
.swarm/

*storybook.log
storybook-static
storybook-static/
# Pantheon API helper — generated docs
.pantheonapi-docs/
